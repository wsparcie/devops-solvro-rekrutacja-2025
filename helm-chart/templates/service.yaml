apiVersion: v1
kind: Service
metadata:
  name: {{ include "cocktail-api.fullname" . }}
  labels:
    {{- include "cocktail-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cocktail-api.selectorLabels" . | nindent 4 }}
